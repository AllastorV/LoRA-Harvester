# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments
venv/
.venv/
env/

# Runtime / user data (regenerated by the app)
.pytest_cache/
.runtime.lock
.setup.lock
crash_log.txt
logs/
venv-backup-*/
data/*.json
output/
.mcp_jobs/

# Downloaded model weights (large, fetched on first run)
models/

# OS / editor
.DS_Store
Thumbs.db

# Local MCP config (machine-specific paths)
.mcp.json

# Installed at runtime by src/training/kohya_installer.py
kohya_ss/

# Live AI session credentials / request logs must never be committed
.ai-control/
logs/ai-control/
